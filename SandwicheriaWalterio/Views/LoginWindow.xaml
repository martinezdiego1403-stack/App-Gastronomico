<Window x:Class="SandwicheriaWalterio.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Sandwichería Walterio - Iniciar Sesión" 
        Height="550" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="White" CornerRadius="15" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barra de título personalizada -->
            <Border Grid.Row="0" Background="#2C3E50" CornerRadius="15,15,0,0" Padding="15,10" MouseLeftButtonDown="Border_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="🥪 Sandwichería Walterio" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
                    <Button x:Name="btnCerrar" Content="✕" HorizontalAlignment="Right" 
                            Background="Transparent" Foreground="White" BorderThickness="0"
                            FontSize="16" Width="30" Height="30" Cursor="Hand"
                            Click="BtnCerrar_Click"/>
                </Grid>
            </Border>

            <!-- Contenido -->
            <StackPanel Grid.Row="1" Margin="40,30,40,40">
                
                <!-- Logo Walterio -->
                <Border Width="100" Height="100" CornerRadius="50" Margin="0,0,0,20" HorizontalAlignment="Center">
                    <Border.Background>
                        <ImageBrush ImageSource="/logo.png" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>

                <!-- Título -->
                <TextBlock Text="Bienvenido" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                <TextBlock Text="Ingresa tu nombre para continuar" FontSize="12" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <!-- Campo Usuario -->
                <TextBlock Text="Nombre de Usuario" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,8"/>
                <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="8" Padding="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <fa:IconImage Grid.Column="0" Icon="User" Width="16" Height="16" Foreground="#BDC3C7"/>
                        <TextBox x:Name="txtUsuario" Grid.Column="1" BorderThickness="0" FontSize="14" Padding="5" 
                                 VerticalContentAlignment="Center" KeyDown="TxtUsuario_KeyDown"/>
                    </Grid>
                </Border>

                <!-- ComboBox de usuarios existentes -->
                <TextBlock Text="O selecciona un usuario:" FontSize="11" Foreground="#95A5A6" Margin="0,15,0,5"/>
                <ComboBox x:Name="cboUsuarios" FontSize="13" Padding="10,8" SelectionChanged="CboUsuarios_SelectionChanged"/>

                <!-- Mensaje de error -->
                <TextBlock x:Name="txtError" Text="" Foreground="#E74C3C" FontSize="12" Margin="0,15,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>

                <!-- Botón Ingresar -->
                <Button x:Name="btnIngresar" Click="BtnIngresar_Click" Margin="0,25,0,0" Height="45" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#27AE60" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <fa:IconImage Icon="SignInAlt" Width="16" Height="16" Foreground="White" Margin="0,0,10,0"/>
                                    <TextBlock Text="INGRESAR" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#219A52"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#1E8449"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Footer -->
                <TextBlock Text="© 2024 Sandwichería Walterio - v1.0" FontSize="10" Foreground="#BDC3C7" HorizontalAlignment="Center" Margin="0,30,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
